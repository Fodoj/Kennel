<div class="row">
    <%= render "sidenav" %>

  <div class="span10">
    <div class="pull-right actions">
      <%= link_to "Редактировать", edit_admin_pet_path(@pet), :class => "btn btn-small", :title => "Редактировать питомца '#{@pet.name}'" %>
    </div>
    <h1>
      <%= @pet.name %>
    </h1>
    <div class="row">
      <div class="span10">

        <%= render  "admin/shared/photos", :photos => @photos %>

      </div>
    </div>
  </div>
</div>
