<%= form_for [:admin, post], :validate => true, :html => { :class => "form-horizontal" } do |f| %>

<div class="row">

      <div class="control-group">
         <label class="control-label">Заголовок</label>
         <div class="controls">
           <%= f.text_field :title, :class => "input-xlarge"  %>
         </div>
      </div>

      <div class="control-group">
         <label class="control-label">Титульное фото</label>
         <div class="controls">
           <%= f.file_field :photo, :class => "input-xlarge"  %>
         </div>
      </div>

      <div class="control-group">
         <label class="control-label">Текст</label>
         <div class="controls">
           <%= f.cktext_area :body, :toolbar => "Basic", :input_html => { :rows => "10", :class => "span5" } %>
         </div>
      </div>

      <div class="control-group">
         <label class="control-label">Прикрепить альбом</label>
         <div class="controls">
           <%= f.select :album_id, Album.all.collect {|p| [ p.name, p.id ] }, { :prompt => "Без альбома"} %>
         </div>
      </div>

</div>

<div class="form-actions">
  <%= f.submit "Сохранить", :class => 'btn btn-primary' %>
</div>

<% end %>
